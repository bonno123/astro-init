name: Deno Deploy
on: [push]

env:
    PUBLIC_SITE_NAME: ${{ vars.PUBLIC_SITE_NAME }}
    PUBLIC_CONTACT_EMAIL: ${{ vars.PUBLIC_CONTACT_EMAIL }}
    ASTRO_STUDIO_APP_TOKEN: ${{ secrets.ASTRO_STUDIO_APP_TOKEN }}

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Needed for auth with Deno Deploy
      contents: read # Needed to clone the repository

    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      # Not using yarn? Change `yarn install` to `npm ci` or `pnpm i`
      - name: Install dependencies
        run: yarn install

      # Not using yarn? Change `npm run build` to `yarn build` or `pnpm run build`
      - name: Build Astro
        run: yarn build

      - name: Upload to Deno Deploy
        uses: denoland/deployctl@v1
        with:
          project: avikbanik-astro-init-25 # TODO: replace with Deno Deploy project name
          entrypoint: server/entry.mjs
          root: dist