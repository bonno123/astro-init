# Local development environment
# This file is loaded by Astro during local development
# For production, use the main .env file with PUBLIC_API_ENDPOINT

# Local API endpoint - routes to /api/* handlers during local dev
PUBLIC_API_ENDPOINT=http://localhost:3000
