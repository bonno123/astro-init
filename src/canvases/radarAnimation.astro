---
import fragmentShader from "~/glsl/radar-animation/index.frag?raw";
---

<canvas
  class="glslCanvas"
  id="indexShaderCanvas"
  data-fragment={fragmentShader}
  data-textures="/mars.png"
>
  Your browser does not support the HTML5 canvas element.
</canvas>

<script>
  import GlslCanvas from "glslCanvas";

  let canvas: HTMLCanvasElement | null = null;
  let sandbox: typeof GlslCanvas | null = null;

  function initializeShader() {
    canvas = document.getElementById("indexShaderCanvas") as HTMLCanvasElement;
    if (canvas) {
      sandbox = new GlslCanvas(canvas);
      canvas.style.width = "100%";
      canvas.style.height = "100%";
    }
  }

  // Event listener for Astro SPA navigation
  document.addEventListener("astro:after-swap", () => {
    initializeShader();
  });

  initializeShader();
</script>
