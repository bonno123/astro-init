---
import { heroWidget } from "~/data/site-info";

const githubLink = heroWidget.socialLinks[0].href;

const { pathname } = Astro.url;

const navItems = [
  { label: "Home", route: "/", icon: "home" },
  { label: "Projects", route: "/projects/", icon: "projects" },
  { label: "Services", route: "/services/", icon: "services" },
  { label: "About", route: "/about/", icon: "about" },
  { label: "Contact", route: "/contact/", icon: "contact" },
];

const icons: Record<string, { viewBox: string; path: string }> = {
  home: {
    viewBox: "0 0 24 24",
    path: "M4.5 10.5 12 4l7.5 6.5v7a1 1 0 0 1-1 1h-5.5v-5h-2v5H5.5a1 1 0 0 1-1-1z",
  },
  projects: {
    viewBox: "0 0 24 24",
    path: "M4 5.5A1.5 1.5 0 0 1 5.5 4h5A1.5 1.5 0 0 1 12 5.5v5A1.5 1.5 0 0 1 10.5 12h-5A1.5 1.5 0 0 1 4 10.5zM13 6h5.5A1.5 1.5 0 0 1 20 7.5v3A1.5 1.5 0 0 1 18.5 12H13zM4 15.5A1.5 1.5 0 0 1 5.5 14h5A1.5 1.5 0 0 1 12 15.5v3A1.5 1.5 0 0 1 10.5 20h-5A1.5 1.5 0 0 1 4 18.5zM13 15h5.5a1.5 1.5 0 0 1 1.5 1.5v2a1.5 1.5 0 0 1-1.5 1.5H13z",
  },
  services: {
    viewBox: "0 0 24 24",
    path: "M12 4a4 4 0 0 1 3.46 2H20v2h-2.1a6.02 6.02 0 0 1-.9 2.15l1.49 1.48-1.42 1.42-1.49-1.49A6 6 0 1 1 6 11a6.02 6.02 0 0 1 .9-2.15L5.41 7.36 6.83 5.94 8.32 7.43A6.02 6.02 0 0 1 11 6.1V4zm0 4a3 3 0 1 0 3 3 3 3 0 0 0-3-3Z",
  },
  about: {
    viewBox: "0 0 24 24",
    path: "M12 12a5 5 0 1 0-5-5 5 5 0 0 0 5 5Zm0 2c-3.33 0-6 1.34-6 3v1h12v-1c0-1.66-2.67-3-6-3Z",
  },
  contact: {
    viewBox: "0 0 24 24",
    path: "M4 5h16a1 1 0 0 1 1 1v12a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1Zm0 2v.38l8 4.62 8-4.62V7Zm16 2.76-7.55 4.36a1 1 0 0 1-.9 0L4 9.76V18h16Z",
  },
};
---

<!-- ========Click Effect====== -->
<div class="nav-bar">
  <div class="position-relative js-morph-bg-wrapper">
    <ul class="demo-morph-bg-list" role="menu">
      {
        navItems.map((item) => (
          <li class="cd-z-index-2">
            <button
              data-route-to={item.route}
              class="demo-morph-bg-button"
              data-morph-bg="morph-bg-2"
              data-morph-bg-active={pathname === item.route}
              aria-label={item.label}
            >
              <span class="nav-icon" aria-hidden="true">
                <svg
                  viewBox={icons[item.icon].viewBox}
                  width="20"
                  height="20"
                  fill="currentColor"
                >
                  <path d={icons[item.icon].path} />
                </svg>
              </span>
              <span class="nav-label">{item.label}</span>
            </button>
          </li>
        ))
      }
    </ul>

    <span
      class="morph-bg cd-z-index-1 demo-morph-bg-2 js-morph-bg"
      id="morph-bg-2"
      aria-hidden="true"></span>
  </div>

  <a
    href={githubLink}
    target="_blank"
    rel="noopener"
    aria-label="GitHub"
    class="github-icon-link"
  >
    <svg
      height="20"
      width="20"
      viewBox="0 0 20 20"
      version="1.1"
      xmlns="http://www.w3.org/2000/svg"
      xmlns:xlink="http://www.w3.org/1999/xlink"
      fill="currentColor"
      ><g id="SVGRepo_bgCarrier" stroke-width="0"></g><g
        id="SVGRepo_tracerCarrier"
        stroke-linecap="round"
        stroke-linejoin="round"></g><g id="SVGRepo_iconCarrier">
        <title>github [#142]</title>
        <desc>Created with Sketch.</desc>
        <defs> </defs>
        <g
          id="Page-1"
          stroke="none"
          stroke-width="1"
          fill="none"
          fill-rule="evenodd"
        >
          <g
            id="Dribbble-Light-Preview"
            transform="translate(-140.000000, -7559.000000)"
            fill="currentColor"
          >
            <g id="icons" transform="translate(56.000000, 160.000000)">
              <path
                d="M94,7399 C99.523,7399 104,7403.59 104,7409.253 C104,7413.782 101.138,7417.624 97.167,7418.981 C96.66,7419.082 96.48,7418.762 96.48,7418.489 C96.48,7418.151 96.492,7417.047 96.492,7415.675 C96.492,7414.719 96.172,7414.095 95.813,7413.777 C98.04,7413.523 100.38,7412.656 100.38,7408.718 C100.38,7407.598 99.992,7406.684 99.35,7405.966 C99.454,7405.707 99.797,7404.664 99.252,7403.252 C99.252,7403.252 98.414,7402.977 96.505,7404.303 C95.706,7404.076 94.85,7403.962 94,7403.958 C93.15,7403.962 92.295,7404.076 91.497,7404.303 C89.586,7402.977 88.746,7403.252 88.746,7403.252 C88.203,7404.664 88.546,7405.707 88.649,7405.966 C88.01,7406.684 87.619,7407.598 87.619,7408.718 C87.619,7412.646 89.954,7413.526 92.175,7413.785 C91.889,7414.041 91.63,7414.493 91.54,7415.156 C90.97,7415.418 89.522,7415.871 88.63,7414.304 C88.63,7414.304 88.101,7413.319 87.097,7413.247 C87.097,7413.247 86.122,7413.234 87.029,7413.87 C87.029,7413.87 87.684,7414.185 88.139,7415.37 C88.139,7415.37 88.726,7417.2 91.508,7416.58 C91.513,7417.437 91.522,7418.245 91.522,7418.489 C91.522,7418.76 91.338,7419.077 90.839,7418.982 C86.865,7417.627 84,7413.783 84,7409.253 C84,7403.59 88.478,7399 94,7399"
                id="github-[#142]"
              >
              </path>
            </g>
          </g>
        </g>
      </g>
    </svg>
    <span class="sr-only">Follow @bonno123</span>
  </a>
</div>

<script>
  import { navigate } from "astro:transitions/client";

  // Navigate to the selected route when clicking on the button
  document.addEventListener("click", (event) => {
    const button = (event.target as Element)?.closest("[data-route-to]");
    if (!button) return;

    const route = button.getAttribute("data-route-to");
    navigate(String(route));
  });

  var customEventRouteChanged = new CustomEvent("route-changed");

  // since the state is set to persist, we need to update the morph-bg-active attribute when the route changes
  document.addEventListener(
    "astro:page-load",
    (event) => {
      setupStuff(event.target as Element); // This runs once the page is loaded.
    },
    { passive: true }
  );

  function setupStuff(target: Element | null) {
    //  change the attribute data-morph-bg-active to true if the pathname matches the button's data-route-to
    // Assuming `pathname` is reactive and part of your component's state
    let routePath = window.location.pathname; // Initialize pathname with the current path
    const buttons = target?.querySelectorAll("[data-route-to]");

    if (!buttons) return;

    buttons.forEach((button) => {
      const routeTo = button.getAttribute("data-route-to");
      button.setAttribute(
        "data-morph-bg-active",
        String(routeTo === routePath)
      );
    });

    doneRouteChange(customEventRouteChanged);
  }

  function doneRouteChange(customEvent: CustomEvent<unknown>) {
    window.dispatchEvent(customEvent);
  }
</script>

<script>
  import MorphBackground from "~/utils/morphBackgroundClass";

  var morphBg = document.getElementsByClassName("js-morph-bg");

  function initMorph() {
    if (morphBg.length > 0) {
      for (var i = 0; i < morphBg.length; i++) {
        const morphInstance = new MorphBackground(morphBg[i] as HTMLElement);
      }
    }
  }

  document.addEventListener("DOMContentLoaded", () => {
    const targetElement = document.querySelector(".demo-morph-bg-button"); // Adjust the selector as needed

    const observer = new IntersectionObserver(
      (entries, observer) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            // Element is now visible
            initMorph();
            // Optionally, stop observing if you only need to initialize once
            observer.unobserve(entry.target);
          }
        });
      },
      {
        root: null, // observing the document viewport
        rootMargin: "0px",
        threshold: 0.1, // Adjust threshold to control when the callback is executed
      }
    );

    // Start observing the target element
    observer.observe(targetElement!);
  });

  // ============== MORPH BG RESIZE ============== //
  // on window resize - reset morph bg size
  var resizingId: string | number | undefined,
    customEventMorph = new CustomEvent("update-morphbg"),
    customEventHide = new CustomEvent("hide-morphbg");

  // on window resize - reset morph bg size
  window.addEventListener("resize", function () {
    if (!resizingId) doneResizing(customEventHide);
    clearTimeout(resizingId as number);
    resizingId = setTimeout(function () {
      doneResizing(customEventMorph);
      resizingId = undefined;
    }, 100) as unknown as number;
  });

  // wait for font to be loaded
  if (document.fonts) {
    document.fonts.onloadingdone = function (fontFaceSetEvent) {
      doneResizing(customEventMorph);
    };

    document.fonts.ready.then(function () {
      setTimeout(function () {
        doneResizing(customEventMorph);
      }, 300);
    });
  }

  function doneResizing(customEvent: CustomEvent<unknown>) {
    window.dispatchEvent(customEvent);
  }
</script>

<style>
  /* -------------------------------- 

Title: Morphing Background
Descr: A morphing background item that transitions from one element's position to another's

-------------------------------- */

  /* --------------------------------

Component 

-------------------------------- */

  .morph-bg {
    position: absolute;
    top: 0;
    left: 0;
    height: 1px;
    width: 1px;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s;
    transform-origin: left top;
    -webkit-backface-visibility: hidden;
    backface-visibility: hidden;
  }

  .morph-bg--visible {
    opacity: 0.28;
  }

  .morph-bg--has-transition {
    transition: 0.3s;
    will-change: transform, border-radius, height, width;
  }

  /* -------------------------------- 

Demo style 

-------------------------------- */

  .demo-morph-bg-paragraph {
    position: relative; /* This makes z-index applicable */
    padding: 1rem;
    font-size: 0.9375rem;
    color: hsl(240, 66%, 63%);
  }

  .demo-morph-bg-list {
    display: inline-flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    overflow: hidden;
    padding: 0.35rem;
    background: var(--clr-surface);
    border-radius: 999px;
    border: 1px solid var(--clr-border);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.06);
    backdrop-filter: blur(12px);
  }

  .demo-morph-bg-link {
    display: block;
    padding: 0.75rem 1rem;
    color: hsl(230, 13%, 9%);
    text-decoration: none;
    font-weight: medium;
  }

  .demo-morph-bg-target {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    pointer-events: none;
  }

  .demo-morph-bg-button {
    border-radius: 999px;
    font-weight: 600;
    cursor: pointer;
    color: var(--clr-neutral);
    padding: 0.65rem 1rem;
    transition: all 0.18s ease;
    border: 1px solid transparent;
    background: transparent;
    position: relative;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
  }

  .nav-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 20px;
    height: 20px;
    color: var(--clr-neutral);
  }

  .nav-label {
    font-size: 0.95rem;
  }

  .demo-morph-bg-button:hover {
    transform: translateY(-1px);
    color: var(--clr-accent);
    border-color: var(--clr-border);
  }

  .demo-morph-bg-button[data-morph-bg-active="true"] {
    color: var(--clr-neutral-dark);
    font-weight: 700;
    border-color: var(--clr-accent);
  }

  @keyframes blur {
    0% {
      filter: blur(0);
      transform: scale(1);
    }
    50% {
      filter: blur(1.15px);
      transform: scale(1.02);
    }
    100% {
      filter: blur(0);
      transform: scale(1.01);
      /* background: #2ecc71; */
    }
  }

  /* .demo-morph-bg-1 {
    background-color: hsl(250, 84%, 54%);
} */

  .demo-morph-bg-2 {
    background-color: hsla(204, 86%, 55%, 0.18);
    box-shadow:
      0 0 0 2px hsla(204, 86%, 55%, 0.25),
      0 10px 30px hsla(204, 86%, 35%, 0.18);
  }

  .nav-bar {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    width: 100%;
  }

  .github-icon-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 1px solid var(--clr-border);
    background: var(--clr-surface);
    color: var(--clr-neutral);
    transition: all 0.2s ease;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
  }

  .github-icon-link:hover {
    color: var(--clr-accent);
    border-color: var(--clr-accent);
    transform: translateY(-1px);
  }

  @media (max-width: 55em) {
    .nav-bar {
      gap: 0.5rem;
    }

    .demo-morph-bg-list {
      width: 100%;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    .demo-morph-bg-list::-webkit-scrollbar {
      display: none;
    }

    .demo-morph-bg-list li {
      scroll-snap-align: center;
    }

    .demo-morph-bg-button {
      padding: 0.6rem 0.75rem;
      gap: 0;
    }

    .nav-label {
      display: none;
    }
  }

  /* -------------------------------- 

Utilities 

-------------------------------- */

  .cd-block {
    display: block;
  }

  .cd-position-relative {
    position: relative;
  }

  .cd-z-index-1 {
    z-index: 1;
  }

  .cd-z-index-2 {
    z-index: 2;
  }

  /* .cd-margin-y-xs {
    margin-block: 1.675rem;
} */

  /* .cd-margin-bottom-sm {
    margin-bottom: 2.5rem;
} */

  /* .cd-margin-bottom-xl {
    margin-bottom: 4.5rem;
} */

  .cd-text-center {
    text-align: center;
  }

  .github-icon-link {
    display: inline-flex;
    align-items: center;
    margin-left: 1rem;
    opacity: 0.6;
    color: var(--clr-neutral);
    transition:
      opacity 0.2s ease,
      color 0.2s ease;
  }

  .github-icon-link:hover {
    opacity: 1;
    color: var(--clr-accent);
  }
</style>
